{% extends 'base.html.twig' %}

{% block body %}
<div class="container hauteurMin">
    <h1 class="mt-5">Edition catégorie</h1>


    {# ouvre la balise form #}
    {{ form_start(form) }}

    {{ form_row(form.type) }}
    {{ form_row(form.image) }}

    <div class="text-right">
        <button type="submit" class="btn btn-primary">Enregistrer</button>
        <a class="btn btn-outline-info" href="{{ path('app_admin_categorie_index') }}">Retour</a>
    </div>

    {# ferme la balise form #}
    {{ form_end(form) }}

</div>

{% endblock %}





